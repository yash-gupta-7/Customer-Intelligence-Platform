// API base URL — empty string uses same origin (Static Web App /api proxy).
// For local dev against docker compose, set: window.CIP_API_BASE = 'http://localhost:8000';
window.CIP_API_BASE = window.CIP_API_BASE || '';
